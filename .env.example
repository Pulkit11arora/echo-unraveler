# Live Gradio (Colab / Space) tunnel URL for the /separate endpoint.
# These URLs are temporary and expire when the notebook session ends.
VITE_GRADIO_ENDPOINT=https://YOUR_TEMPORARY_HASH.gradio.live
